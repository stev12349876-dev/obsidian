# Day 01：理解语言模型的本质

> 目标：建立对语言模型、分词、训练流程的基本认知。不跳步，每个概念都解释。

---

## 一、语言模型的核心任务

语言模型只做一件事：**给定一段上文，预测下一个字**。

```
输入：  "今天天气真"
输出：  "好"（概率 60%）、"热"（20%）、"冷"（10%）、"差"（5%）、...
```

这不是什么复杂逻辑——就是一个"接龙"。但神奇之处在于：**当接龙正确率足够高时，模型自然就学会了语言中的语法、知识、推理。**

为什么？因为互联网上的所有文字——维基百科、代码仓库、对话记录、学术论文——本质上都是"前缀 → 后缀"的映射。模型看多了，就内化了这些模式：

- 语法模式：看到"我昨天"后面大概率接动词
- 事实模式：看到"巴黎是"后面大概率接"法国的首都"
- 推理模式：看到"1+1="后面大概率接"2"

模型并不"理解"这些知识——它只是统计出了最可能的接龙方式。但这个统计结果表现得像"理解"一样。

---

## 二、训练：如何让模型学会接龙

训练 = 反复做"猜字 → 看答案 → 纠正自己"。

**举例：** 模型看到"今天天气"，要猜下一个字。

```
第 1 次：模型瞎猜 → 猜"狗"（错）→ 正确答案是"真"
第 2 次：模型调整后 → 猜"好"（错）→ 正确答案还是"真"
...
第 N 次：模型终于学会 → 猜"真"（对）
```

但模型不是只记"今天天气 → 真"这一条。它有 **6400 万个参数**（可调节的数值），这些参数共同编码了所有语言模式。训练就是不断微调这些参数，让整体猜对率越来越高。

衡量猜得准不准的指标叫 **loss（损失）**：

```
loss = -log(模型对正确答案给出的概率)

模型很自信（概率 90%）→ loss ≈ 0.1  （低，好）
模型瞎猜（概率 0.02%）→ loss ≈ 8.8  （高，差）

训练目标：把 loss 从 ~8.8 降到 ~2.0
```

---

## 三、Tokenizer：文字如何变成模型能处理的数字

模型不认识汉字，只认识数字。Tokenizer 就是翻译官。

MiniMind 使用 **BPE（Byte Pair Encoding）** 算法训练了一个 6400 词（token）的词典。训练过程如下：

**第一步：** 统计训练文本中所有相邻字符对的频率。

**第二步：** 把出现最多的那一对合并成一个新词。

**第三步：** 重复，直到词表达到 6400 个。

举例：

假设训练文本中有很多这样的搭配：
```
"人工" 出现了 500 次
"智能" 出现了 600 次
"人工智能" 这个四字组合出现了 400 次
```

BPE 会先合并"人工"（高频），再合并"智能"，最终"人工智能"可能只需要 2 个 token（"人工" + "智能"），而不是 4 个。

实际测试效果：

```
"你好世界" → [1968, 1950] → ['你好', '世界']   （2 个 token，压缩率 2.0）
"Hello World" → [1602, 2968] → ['Hello', ' World']   （2 个 token，压缩率 5.5）
"GPT-4是OpenAI开发的" → ['G','P','T','-','4','是','O','pen','AI','开发','的']   （11 个 token）
```

最后一行值得注意：BPE 不认识"GPT-4"和"OpenAI"这种专有名词（出现频率太低，达不到合并阈值），所以拆成了零件。虽然 token 数量变多了，但这是有意为之——如果频繁出现的专有名词都要单独存一个 token，词表会膨胀到无法接受。

Tokenizer 还有一个重要功能：用**特殊标记**区分对话角色。这是 MiniMind 使用的对话格式：

```
<|im_start|>user
你是谁<|im_end|>
<|im_start|>assistant
我是MiniMind<|im_end|>
```

当模型看到 `<|im_start|>assistant`，它就"知道"：现在轮到我说话了，我需要生成回复，直到遇见 `<|im_end|>` 就停下来。模型不知道什么叫"角色"，它只是通过模式学会了特殊标记和回复之间的对应关系。

---

## 四、数据：模型靠什么学习

MiniMind 项目提供了三种训练数据，对应三个训练阶段：

### 预训练数据（pretrain_t2t_mini.jsonl）

127 万条纯文本，没有任何角色标记。每条都是一段连续文字：

```json
{"text": "给我生成一首有关秋天的诗歌。秋日早晨，清风拂面。金色的叶子..."}
{"text": "根据以下输入的问题，生成一句话回答。你觉得寿司好不好吃？..."}
```

这些数据用来做**预训练（Pretrain）**——让模型学会语言能力。相当于"读书识字"阶段。

### SFT 数据（sft_t2t_mini.jsonl）

多轮对话范例，有明确的角色标记：

```json
{"conversations": [
    {"role": "user", "content": "你背后的模型是哪个版本？"},
    {"role": "assistant", "content": "我是由jingyaogong开发的高效小参数AI模型。"}
]}
```

这些数据用来做**监督微调（SFT）**——让模型学会对话格式。相当于"学礼仪"阶段。

### 偏好数据（rlaif.jsonl）

包含"好回答"和"差回答"的对比：

```json
// 好回答：具体、有帮助
{"role": "assistant", "content": "首先明确目标职位，用具体数据展示成果..."}

// 差回答：敷衍、无帮助
{"role": "assistant", "content": "随便写写就行，反正都差不多。"}
```

这些数据用来做**偏好对齐（DPO）**——让模型学会什么是"更好的回答"。

---

## 五、训练的三阶段流程

```
阶段 1：Pretrain（预训练）
  数据：127 万条纯文本
  目标：学会语言基本规律（语法、常识、推理）
  结果：能做"接龙"，但不知道自己是助手

阶段 2：SFT（监督微调）
  数据：多轮对话范例
  目标：学会对话格式和指令跟随
  结果：能作为助手回答问题

阶段 3：DPO（偏好对齐）
  数据：好回答 vs 差回答
  目标：让回答更符合人类偏好
  结果：回答更自然、更有帮助、更安全
```

**为什么不能跳过 Pretrain 直接做 SFT？**

因为 Pretrain 教的是"语言能力"，SFT 教的是"对话礼仪"。前者需要海量数据（127 万条）来建立对语法、常识、逻辑的基本认知；后者只需要少量对话范例来学习格式。如果直接用对话数据训练，模型连"你"是什么意思都不清楚，只能机械地模仿对话模板。

---

## 六、模型的核心配置：这些数字是什么意思

MiniMind 的配置定义在 `model/model_minimind.py` 的 `MiniMindConfig` 类中。每个数字都不是随便选的：

| 参数 | 值 | 含义 |
|------|-----|------|
| `vocab_size` | 6400 | 词表大小：模型认识的"字/词"总数 |
| `hidden_size` | 768 | 每个字在模型内部的"表示维度"——用 768 个小数来描述一个字 |
| `num_hidden_layers` | 8 | 模型"深度"——信息反复加工 8 次 |
| `num_attention_heads` | 8 | 注意力"头数"——同时从 8 个角度看文本 |

**hidden_size = 768 是怎么来的？**

Transformer 架构有一个标准化的维度序列：512 → 768 → 1024 → 2048 → 4096。768 是"够用但最小"的标准选择——它必须能被 8（注意力头数）和 64（GPU 内存对齐单位）整除。如果改成 512，表达能力下降明显；改成 1024，参数翻倍但收益递减。

**为什么需要 8 层？**

每一层对信息做一次"深入加工"。只做 1 层就像看了一眼就回答——容易出错。做 8 层意味着"反复推敲 8 次"——对于 64M 的小模型来说，这个深度刚好够学会完整的中英文，又不会让参数超标。

**6400 万参数是怎么分布的？**

```
总参数：63.91M
├── Embedding（词嵌入）：   4.92M（ 7.7%）  ← 6400 个字，每个字 768 维
├── Attention（注意力）：  14.16M（22.2%）  ← 找"谁跟谁有关"的能力
├── FeedForward（前馈层）： 44.83M（70.1%）  ← 存储知识和常识的"记忆库"
└── 其他（归一化等）：      0.02M（ 0.0%）
```

注意：70% 的参数都在前馈层（FeedForward）中，而不是更常被提到的注意力层（Attention）。这是因为注意力层负责"找关系"（谁和谁相关），前馈层负责"存知识"（所有学到的事实、概念、模式）。存储知识需要大量容量。

---

## 七、项目目录一览

```
minimind/
├── model/              ← 模型的定义代码（288行，纯手写，不依赖第三方库）
│   ├── model_minimind.py     ← Config、Attention、FFN、完整模型
│   ├── model_lora.py         ← LoRA 微调模块
│   ├── tokenizer.json        ← 6400 词的 BPE 词表
│   └── tokenizer_config.json ← 分词器配置和对话模板
│
├── trainer/            ← 训练脚本（每个阶段一个文件）
│   ├── train_pretrain.py     ← Pretrain 训练
│   ├── train_full_sft.py     ← SFT 训练
│   ├── train_dpo.py          ← DPO 训练
│   └── trainer_utils.py      ← 共用工具函数
│
├── dataset/            ← 训练数据
│   ├── pretrain_t2t_mini.jsonl  ← 127万条纯文本
│   ├── sft_t2t_mini.jsonl       ← 多轮对话
│   └── rlaif.jsonl              ← 偏好数据
│
├── scripts/            ← 推理脚本（让模型说话）
│   └── chat_terminal.py  ← 命令行对话
│
└── weights/minimind-3/ ← 已训练好的模型权重（128MB）
```

---

## 八、动手：让你的 MiniMind 说第一句话

确保数据集已下载（在 `dataset/` 目录下），然后运行：

```bash
cd /Users/xiaoxiaoma/Documents/minimind
python scripts/chat_terminal.py
```

你会看到：

```
🤖 正在加载 MiniMind (64M)...
✅ 加载完成 (CPU)

👤 你: 你好
🤖 MiniMind: 你好！我是MiniMind，有什么可以帮助你的吗？
```

这 4 行核心代码是：

```python
model = AutoModelForCausalLM.from_pretrained("weights/minimind-3")  # 加载模型
tokenizer = AutoTokenizer.from_pretrained("weights/minimind-3")     # 加载分词器

text = tokenizer.apply_chat_template(messages, add_generation_prompt=True)  # 对话→文本
inputs = tokenizer(text)          # 文本→数字
outputs = model.generate(**inputs)  # 模型生成
response = tokenizer.decode(outputs[0])  # 数字→文本
```

---

## 今天你学到的

| 概念 | 一句话 |
|------|--------|
| 语言模型 | 给定上文猜下文，靠"接龙"学会语言能力 |
| 参数 | 6400 万个可调节数值，训练就是调整它们 |
| Tokenizer | 文字↔数字的翻译官，BPE 算法自动合并高频字对 |
| Pretrain | 用 127 万条纯文本学会基本语言能力 |
| SFT | 用对话范例学会"回答问题"的格式 |
| DPO | 用偏好对比学会"更好的回答" |
| 6400 词 × 768 维 | 每个字用 768 个小数表示 |

---

## 下一天：Transformer 的核心机制

我们将深入模型内部，理解：
- 什么是 Embedding：一个字如何变成一个 768 维"含义向量"
- 什么是 Attention：模型如何知道"她"指的是"小红"而不是"小明"
- 什么是层：为什么信息要加工 8 次
